import React from "react";
import "./Spinner.css";
// wait till page loading it shows processing
const Spinner = () => {
  return <div className="loader">Loading...</div>;
};

export default Spinner;
